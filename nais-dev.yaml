apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: soknadsveiviser
  namespace: default
  labels:
    team: personbruker
spec:
  image: navikt/soknadsveiviser:4
  team: personbruker
  port: 8080
  ingresses:
    - https://soknader.nais.oera-q.local
    - https://www-q0.nav.no/soknader
  liveness:
    path: /soknader/isAlive
    initialDelay: 20
  readiness:
    path: /soknader/isReady
    initialDelay: 20
  secrets: true
  replicas:
    min: 1
    max: 2
    cpuThresholdPercentage: 90
  webproxy: true
  env:
    - name: NODE_EXTRA_CA_CERTS
      value: /etc/ssl/ca-bundle.pem
  resources:
    limits:
      cpu: "3"
      memory: 768Mi
    requests:
      memory: 512Mi

